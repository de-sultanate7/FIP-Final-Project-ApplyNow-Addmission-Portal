module.exports = {
  plugins: {
    // Tailwind's PostCSS plugin is now provided by '@tailwindcss/postcss'
    // install it and use the package name here.
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
}
